import { PrintLegendDirective } from './print-legend.directive';

describe('PrintLegendDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintLegendDirective();
    expect(directive).toBeTruthy();
  });
});
